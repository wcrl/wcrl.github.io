# frozen_string_literal: true

# vvv this is only necassary for windows
gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw]
gem 'wdm', '>= 0.1.0' if Gem.win_platform?
gem 'base64'
gem 'csv'
gem 'bigdecimal'

source "https://rubygems.org"

gemspec

